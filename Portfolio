<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>V. Nagaraj | Full Stack Developer Portfolio</title>
    <!-- Icons and Fonts -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/js/all.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary: #0f172a;    /* Dark Navy */
            --accent: #3b82f6;     /* Bright Blue */
            --gold: #fbbf24;       /* Certificate Gold */
            --text-main: #1e293b;
            --text-muted: #64748b;
            --bg-light: #f8fafc;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Inter', sans-serif; background-color: var(--bg-light); color: var(--text-main); line-height: 1.6; }
        
        /* Navigation */
        nav { background: rgba(255, 255, 255, 0.9); backdrop-filter: blur(10px); padding: 1rem 5%; position: fixed; width: 100%; top: 0; z-index: 1000; display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid #e2e8f0; }
        nav .logo { font-weight: 700; font-size: 1.2rem; color: var(--primary); }
        nav ul { display: flex; list-style: none; gap: 2rem; }
        nav ul a { text-decoration: none; color: var(--text-main); font-weight: 500; font-size: 0.9rem; transition: 0.3s; }
        nav ul a:hover { color: var(--accent); }

        /* Hero Section */
        .hero { height: 80vh; display: flex; align-items: center; justify-content: center; text-align: center; background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%); color: white; padding: 0 20px; }
        .hero h1 { font-size: 3.5rem; margin-bottom: 1rem; }
        .hero p { font-size: 1.25rem; color: #94a3b8; max-width: 600px; margin: 0 auto 2rem; }
        .btn { background: var(--accent); color: white; padding: 0.8rem 2rem; border-radius: 50px; text-decoration: none; font-weight: 600; transition: 0.3s; border: none; cursor: pointer; }
        .btn:hover { background: #2563eb; transform: translateY(-2px); }

        /* Section Styling */
        section { padding: 100px 10%; }
        .section-title { font-size: 2rem; margin-bottom: 3rem; text-align: center; position: relative; }
        .section-title::after { content: ''; width: 60px; height: 4px; background: var(--accent); position: absolute; bottom: -10px; left: 50%; transform: translateX(-50%); }

        /* Grid Layouts */
        .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem; }

        /* Education & Certification Cards */
        .card { background: white; padding: 2rem; border-radius: 12px; border: 1px solid #e2e8f0; transition: 0.3s; }
        .card:hover { border-color: var(--accent); box-shadow: 0 10px 20px rgba(0,0,0,0.05); }
        .card h3 { margin-bottom: 0.5rem; color: var(--primary); }
        .tag { display: inline-block; padding: 0.2rem 0.8rem; background: #dbeafe; color: #1e40af; border-radius: 4px; font-size: 0.75rem; font-weight: 700; margin-bottom: 1rem; }

        /* Skills Section */
        .skills-container { display: flex; flex-wrap: wrap; gap: 10px; justify-content: center; }
        .skill-badge { background: white; border: 1px solid #e2e8f0; padding: 10px 20px; border-radius: 8px; display: flex; align-items: center; gap: 10px; font-weight: 500; }
        .skill-badge i { color: var(--accent); }

        /* Certificate Details */
        .cert-info-list { list-style: none; margin-top: 1rem; }
        .cert-info-list li { margin-bottom: 8px; font-size: 0.9rem; color: var(--text-muted); }
        .cert-info-list b { color: var(--text-main); }

        /* Footer */
        footer { background: var(--primary); color: white; padding: 4rem 10%; text-align: center; }
        .social-icons { margin: 2rem 0; display: flex; justify-content: center; gap: 1.5rem; }
        .social-icons a { color: white; font-size: 1.5rem; transition: 0.3s; }
        .social-icons a:hover { color: var(--accent); }

        @media (max-width: 768px) {
            .hero h1 { font-size: 2.5rem; }
            nav ul { display: none; }
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo">V. NAGARAJ</div>
        <ul>
            <li><a href="#about">About</a></li>
            <li><a href="#skills">Skills</a></li>
            <li><a href="#study">Education</a></li>
            <li><a href="#certificate">Certification</a></li>
        </ul>
        <a href="mailto:yourname@email.com" class="btn" style="padding: 0.5rem 1rem; font-size: 0.8rem;">Hire Me</a>
    </nav>

    <section class="hero">
        <div>
            <h1>V. Nagaraj</h1>
            <p>Certified Full Stack Developer specialized in building scalable web applications and modern user interfaces.</p>
            <div style="display: flex; gap: 1rem; justify-content: center;">
                <a href="#certificate" class="btn">View Certificate</a>
                <a href="#study" class="btn" style="background: transparent; border: 2px solid white;">Education</a>
            </div>
        </div>
    </section>

    <!-- Skills Section -->
    <section id="skills">
        <h2 class="section-title">Technical Skills</h2>
        <div class="skills-container">
            <div class="skill-badge"><i class="fab fa-html5"></i> HTML5</div>
            <div class="skill-badge"><i class="fab fa-css3-alt"></i> CSS3</div>
            <div class="skill-badge"><i class="fab fa-js"></i> JavaScript</div>
            <div class="skill-badge"><i class="fab fa-react"></i> React.js</div>
            <div class="skill-badge"><i class="fab fa-node-js"></i> Node.js</div>
            <div class="skill-badge"><i class="fas fa-database"></i> SQL / MongoDB</div>
            <div class="skill-badge"><i class="fab fa-git-alt"></i> Git & GitHub</div>
        </div>
    </section>

    <!-- Study / Education Section -->
    <section id="study" style="background: #fff;">
        <h2 class="section-title">Education (Study)</h2>
        <div class="grid">
            <div class="card">
                <span class="tag">2021 — 2025 (Expected)</span>
                <h3>Bachelor of Engineering</h3>
                <p><strong>Your College Name Here</strong></p>
                <p style="color: var(--text-muted); margin-top: 10px;">Focused on Computer Science, Algorithms, and Software Engineering principles. Maintaining a strong academic record.</p>
            </div>
            <div class="card">
                <span class="tag">2019 — 2021</span>
                <h3>Higher Secondary Education</h3>
                <p><strong>Your School Name Here</strong></p>
                <p style="color: var(--text-muted); margin-top: 10px;">Completed with a focus on Mathematics and Computer Science.</p>
            </div>
        </div>
    </section>

    <!-- Detailed Certification Section -->
    <section id="certificate">
        <h2 class="section-title">Professional Certification</h2>
        <div class="grid" style="grid-template-columns: 1.5fr 1fr; align-items: center;">
            <div>
                <img src="cert.jpg" alt="Certificate" style="width: 100%; border-radius: 10px; box-shadow: 0 20px 40px rgba(0,0,0,0.1); border: 1px solid #ddd;">
            </div>
            <div class="card">
                <div class="tag" style="background: #fef3c7; color: #92400e;">ISO 9001:2015 Certified</div>
                <h3>Full Stack Development MasterClass</h3>
                <p>Issued by <strong>NoviTech R&D Private Limited</strong></p>
                
                <ul class="cert-info-list">
                    <li><i class="fas fa-check-circle" style="color: green;"></i> ID: <b>NT_B49FSDT273</b></li>
                    <li><i class="fas fa-calendar-alt"></i> Duration: <b>30 Days</b></li>
                    <li><i class="fas fa-clock"></i> Timeline: <b>Oct 09 - Nov 19, 2025</b></li>
                    <li><i class="fas fa-map-marker-alt"></i> Location: <b>Coimbatore</b></li>
                </ul>

                <div style="margin-top: 20px; border-top: 1px solid #eee; padding-top: 15px;">
                    <p style="font-size: 0.8rem; font-style: italic; color: #666;">
                        Verified by: Mr. S. Bharani Chandar (Software Developer) & Mr. A. Vinothkumar (Director)
                    </p>
                </div>
            </div>
        </div>
    </section>

    <footer>
        <h2>V. NAGARAJ</h2>
        <p>Full Stack Developer | Problem Solver | Continuous Learner</p>
        <div class="social-icons">
            <a href="#"><i class="fab fa-linkedin"></i></a>
            <a href="#"><i class="fab fa-github"></i></a>
            <a href="#"><i class="fab fa-twitter"></i></a>
            <a href="mailto:yourname@email.com"><i class="fas fa-envelope"></i></a>
        </div>
        <p style="font-size: 0.8rem; opacity: 0.5;">&copy; 2025 V. Nagaraj. All details sourced from professional credentials.</p>
    </footer>

</body>
</html>
